
export const baseUrl = `https://emp-management-backend-ruby.vercel.app`;

export const authUrl = `${baseUrl}`;
export const departmentUrl = `${baseUrl}`;
export const employeeUrl = `${baseUrl}`;
export const leaveUrl = `${baseUrl}`;
export const salaryUrl = `${baseUrl}`;
export const settingUrl = `${baseUrl}`;
export const dashboardUrl = `${baseUrl}`;




